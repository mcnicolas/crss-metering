spring:
  profiles:
    active: dev
  application:
    name: crss-metering
  cloud:
    config:
      fail-fast: true
      uri: http://config-server:8080
      username: crssconfig
      password: crssconfigpassword
---
spring:
  profiles: dev
  cloud:
    config:
      uri: http://localhost:8888